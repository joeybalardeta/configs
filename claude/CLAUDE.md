Rules to adhere to:

1. Keep it simple and robust - code should be readable and written in a way that promotes stability.

2. Reuse code - when operating on an established codebase, make sure to look for functionality that already exists

3. Don't invent extra layers - if layer is responsible for a task you're working on, don't add another layer doing the same thing.